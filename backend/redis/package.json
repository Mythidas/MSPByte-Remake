{
  "name": "@workspace/redis",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cd .. && docker-compose up -d redis",
    "stop": "cd .. && docker-compose stop redis",
    "logs": "cd .. && docker-compose logs -f redis"
  }
}

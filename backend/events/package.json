{
  "name": "@workspace/events",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "cd .. && docker-compose up -d nats",
    "start": "cd .. && docker-compose up -d nats",
    "stop": "cd .. && docker-compose stop nats",
    "logs": "cd .. && docker-compose logs -f nats"
  }
}

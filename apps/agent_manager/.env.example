# Convex Configuration
# Use the same Convex URL as the main frontend app
NEXT_PUBLIC_CONVEX_URL=

# Convex Secret for bypassing authentication
# This secret allows the app to use the *_s helper variants to bypass auth
NEXT_PUBLIC_CONVEX_SECRET=

# Encryption Key for HaloPSA credentials
# Required for the HaloPSA sync functionality
ENCRYPTION_KEY=

# =============================================================================
# VERCEL DEPLOYMENT - Environment Variables Configuration
# =============================================================================
#
# Add these environment variables in your Vercel project settings:
# https://vercel.com/[your-team]/[your-project]/settings/environment-variables
#
# For local development, copy this file to .env and fill in the values
# =============================================================================

# Application Configuration
NEXT_PUBLIC_ORIGIN=https://your-domain.vercel.app
NEXT_SECRET_KEY=your-secret-key-here

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
NEXT_SUPABASE_API_KEY=your-service-role-key-here

# Microsoft 365 Integration
NEXT_PUBLIC_MICROSOFT_CLIENT_ID=your-client-id-here
NEXT_MICROSOFT_SECRET=your-client-secret-here

# NATS Configuration (if using message queue)
# Note: For Vercel, you'll need to use a hosted NATS service
NATS_URL=nats://your-nats-server:4222

# =============================================================================
# VERCEL DEPLOYMENT NOTES:
# =============================================================================
#
# 1. All NEXT_PUBLIC_* variables are exposed to the browser
# 2. Server-only variables (without NEXT_PUBLIC_) are only available in API routes
# 3. Set Production, Preview, and Development environments separately in Vercel
# 4. Never commit .env files with real credentials to git
#
